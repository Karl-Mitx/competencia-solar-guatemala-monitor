<form method="POST" action="{{ $panel->exists?route('panels.update',$panel):route('panels.store') }}" class="form-layout" data-submit-form>@csrf @if($panel->exists) @method('PUT') @endif<div class="card form-card"><h2>Especificaciones del panel</h2><p class="muted">Consulta estos datos en la ficha técnica del fabricante.</p><div class="form-grid"><label class="field">Marca *<input name="brand" required maxlength="100" value="{{ old('brand',$panel->brand) }}" placeholder="Ej. Canadian Solar"></label><label class="field">Modelo *<input name="model" required maxlength="100" value="{{ old('model',$panel->model) }}" placeholder="Ej. HiKu6 CS6W"></label><label class="field full">Potencia nominal (kW) *<input type="number" name="nominal_power_kw" required min="0.001" max="100" step="0.001" value="{{ old('nominal_power_kw',$panel->nominal_power_kw) }}" placeholder="0.550"><small>Ejemplo: 550 W equivalen a 0.550 kW.</small></label></div><input type="hidden" name="is_active" value="0"><label class="checkbox-label"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$panel->is_active??true))><span><strong>Disponible para instalaciones</strong><small>Permite asociar este modelo a nuevas granjas.</small></span></label>@if($panel->exists)<div class="notice neutral"><x-icon name="info"/><span>Cambiar la potencia modifica la capacidad instalada actual de las granjas que usan este modelo.</span></div>@endif<div class="form-actions"><a class="button button-white" href="{{ route('panels.index') }}">Cancelar</a><button class="button button-dark"><x-icon name="check"/> Guardar modelo</button></div></div><aside class="form-aside"><div class="soft-info"><x-icon name="sun"/><h2>De vatios a kilovatios</h2><p>Divide la potencia en vatios entre 1,000. Mantén la misma unidad en todo el catálogo para obtener capacidades consistentes.</p><div class="formula">550 W ÷ 1,000 = <strong>0.550 kW</strong></div></div></aside></form>
