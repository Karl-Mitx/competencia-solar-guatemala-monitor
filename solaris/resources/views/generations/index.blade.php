@extends('layouts.app')
@section('title','Generación energética')
@section('content')
<div class="page-heading"><div><div class="eyebrow"><span class="eyebrow-line"></span> EL PULSO DE TUS GRANJAS</div><h1>Energía, mes a mes<span class="heading-dot">.</span></h1><p>Compara la producción real con las metas de cada instalación.</p></div><a class="button button-dark" href="{{ auth()->check()?route('generations.create'):route('login') }}"><x-icon name="plus"/> {{ auth()->check()?'Registrar generación':'Ingresar para editar' }}</a></div>
<form class="filter-bar" method="GET"><div class="filter-heading"><x-icon name="filter"/><span>Filtrar por</span></div><label class="farm-filter"><span class="sr-only">Granja solar</span><select name="solar_farm_id"><option value="">Todas las granjas</option>@foreach($farms as $f)<option value="{{ $f->id }}" @selected(($filters['solar_farm_id']??'')==$f->id)>{{ $f->name }}</option>@endforeach</select></label><div class="date-range"><label><span>Desde</span><input name="from" type="month" value="{{ $filters['from']??'' }}"></label><span>—</span><label><span>Hasta</span><input name="to" type="month" value="{{ $filters['to']??'' }}"></label></div><button class="button button-dark button-sm">Aplicar</button><a href="{{ route('generations.index') }}" class="filter-reset" aria-label="Limpiar filtros"><x-icon name="close"/></a></form>
<section class="card"><div class="card-heading"><div><h2>Registros de generación <span class="count-badge">{{ $records->total() }}</span></h2><p>Un registro por granja y mes cerrado. Energía en kWh.</p></div></div><div class="table-scroll"><table><thead><tr><th>Granja solar</th><th>Período</th><th class="number">Real · kWh</th><th class="number">Esperada · kWh</th><th class="number">CO₂ evitado · kg</th><th>Desempeño</th><th></th></tr></thead><tbody>@forelse($records as $r)<tr><td><a class="table-name" href="{{ route('farms.show',$r->solarFarm) }}">{{ $r->solarFarm->name }}</a><small class="block muted">{{ $r->solarFarm->department->name }}</small></td><td>{{ $r->period->format('m/Y') }}</td><td class="number">{{ number_format($r->real_kwh,2) }}</td><td class="number">{{ number_format($r->expected_kwh,2) }}</td><td class="number">{{ number_format($r->real_kwh*.4,2) }}</td><td>@if($r->expected_kwh>0)<span class="pill {{ $r->real_kwh<=$r->expected_kwh*.8?'warning':'positive' }}">{{ number_format($r->real_kwh/$r->expected_kwh*100,1) }}%</span>@else<span class="pill neutral">Sin base</span>@endif</td><td>@auth<a class="icon-button" href="{{ route('generations.edit',$r) }}" aria-label="Editar generación de {{ $r->solarFarm->name }} en {{ $r->period->format('m/Y') }}"><x-icon name="edit"/></a>@endauth</td></tr>@empty<tr><td colspan="7"><div class="empty-state"><x-icon name="bolt"/><h3>No hay registros en este período</h3><p>Ajusta tus filtros o registra la generación del último mes.</p></div></td></tr>@endforelse</tbody></table></div>{{ $records->withQueryString()->links('components.pagination') }}</section>
@endsection
