<form method="POST" action="{{ $record->exists?route('generations.update',$record):route('generations.store') }}" class="form-layout" data-submit-form>@csrf @if($record->exists) @method('PUT') @endif<div class="card form-card"><h2>Registro mensual</h2><p class="muted">Las alertas y el impacto de CO₂ se calculan automáticamente.</p><div class="form-grid"><label class="field full">Granja solar *<select name="solar_farm_id" required><option value="">Selecciona una granja</option>@foreach($farms as $f)<option value="{{ $f->id }}" @selected(old('solar_farm_id',$record->solar_farm_id??request('solar_farm_id'))==$f->id)>{{ $f->name }} · {{ $f->department->name }}</option>@endforeach</select></label><label class="field full">Período *<input name="period" type="month" max="{{ now()->subMonthNoOverflow()->format('Y-m') }}" required value="{{ old('period',$record->period?->format('Y-m')) }}"><small>Selecciona un mes cerrado; no se aceptan meses actuales o futuros.</small></label><label class="field">Generación real (kWh) *<input type="number" name="real_kwh" min="0" max="999999999999" step="0.01" required value="{{ old('real_kwh',$record->real_kwh) }}" placeholder="0.00" data-energy-real></label><label class="field">Generación esperada (kWh) *<input type="number" name="expected_kwh" min="0" max="999999999999" step="0.01" required value="{{ old('expected_kwh',$record->expected_kwh) }}" placeholder="0.00" data-energy-expected></label></div><div class="form-actions"><a class="button button-white" href="{{ route('generations.index') }}">Cancelar</a><button class="button button-dark"><x-icon name="check"/> Guardar generación</button></div></div><aside class="form-aside"><div class="soft-info"><x-icon name="leaf"/><h2>El impacto de este registro</h2><div class="live-stat"><span>CO₂ evitado estimado</span><strong data-co2-preview>0.00 <small>kg</small></strong></div><div class="live-stat"><span>Cumplimiento esperado</span><strong data-performance-preview>—</strong></div><p data-alert-preview>Una generación igual o inferior al 80% de la esperada activa una alerta.</p></div></aside></form>
