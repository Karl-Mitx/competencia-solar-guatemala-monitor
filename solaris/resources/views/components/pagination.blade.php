@if($paginator->hasPages())<nav class="pagination" aria-label="Paginación"><span class="muted">{{ $paginator->firstItem() }}–{{ $paginator->lastItem() }} de {{ $paginator->total() }}</span><div>@if($paginator->onFirstPage())<span class="page-link disabled">Anterior</span>@else<a class="page-link" href="{{ $paginator->previousPageUrl() }}">Anterior</a>@endif @foreach($elements as $element)@if(is_string($element))<span class="page-link disabled">{{ $element }}</span>@else @foreach($element as $page=>$url)<a href="{{ $url }}" class="page-link {{ $page==$paginator->currentPage() ? 'current' : '' }}" @if($page==$paginator->currentPage()) aria-current="page" @endif>{{ $page }}</a>@endforeach @endif @endforeach @if($paginator->hasMorePages())<a class="page-link" href="{{ $paginator->nextPageUrl() }}">Siguiente</a>@else<span class="page-link disabled">Siguiente</span>@endif</div></nav>@endif
